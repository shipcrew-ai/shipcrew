---
name: Add User model to Prisma schema and link to Project
status: open
created: 2026-03-03T05:27:45Z
updated: 2026-03-03T05:30:49Z
github: https://github.com/shipcrew-ai/shipcrew/issues/14
depends_on: []
parallel: true
conflicts_with: [18]
---

# Task: Add User model to Prisma schema and link to Project

## Description
Add a User model to the Prisma schema and add a `userId` foreign key to the existing Project model. Run `prisma db push` to apply.

## Acceptance Criteria
- [ ] User model exists with fields: id (uuid), email (unique), passwordHash, name, createdAt
- [ ] Project model has `userId String` and `user User @relation(...)` fields
- [ ] `prisma db push` succeeds against SQLite
- [ ] Prisma client regenerated with User model available

## Technical Details
- File: `backend/prisma/schema.prisma`
- Add User model with `projects Project[]` relation
- Add `userId String` and `user User @relation(fields: [userId], references: [id])` to Project
- userId should NOT be required initially (nullable) to avoid breaking existing seed — will be required after seed is updated in task 005
- Run: `npm run db:push -w backend`

## Dependencies
- None

## Effort Estimate
- Size: XS
- Hours: 0.5
- Parallel: true
