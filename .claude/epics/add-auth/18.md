---
name: Scope projects to user, update seed, update .env.example
status: open
created: 2026-03-03T05:27:45Z
updated: 2026-03-03T05:30:49Z
github: https://github.com/shipcrew-ai/shipcrew/issues/18
depends_on: [17]
parallel: false
conflicts_with: [14, 17]
---

# Task: Scope projects to user, update seed, update .env.example

## Description
Update projects.ts to filter/associate projects by authenticated user. Update the seed script to create a default user and link the seeded project. Add JWT_SECRET to .env.example.

## Acceptance Criteria
- [ ] `GET /api/projects` filters by `req.user.id` — users only see their own projects
- [ ] `POST /api/projects` sets `userId: req.user.id` on created project
- [ ] Seed script creates a default user (email: `admin@shipcrew.dev`, password: `password123`, name: `Admin`)
- [ ] Seeded project is associated with the default user
- [ ] `.env.example` includes `JWT_SECRET=shipcrew-dev-secret-change-in-production`
- [ ] Full curl test flow passes: register → login → create project → list projects

## Technical Details
- File: `backend/src/api/projects.ts`
  - GET `/`: add `where: { userId: req.user!.id }` to `prisma.project.findMany`
  - POST `/`: add `userId: req.user!.id` to `prisma.project.create` data
- File: `backend/src/db/seed.ts`
  - Import `hashPassword` from auth/hash.ts
  - Create or find default user before creating project
  - Set `userId` on project creation
- File: `.env.example`
  - Add `JWT_SECRET=shipcrew-dev-secret-change-in-production` in a new Auth section

## Dependencies
- Task 004 (auth wired into server)

## Effort Estimate
- Size: S
- Hours: 1
- Parallel: false
