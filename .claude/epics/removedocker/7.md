---
name: Remove Redis and update infrastructure config
status: open
created: 2026-03-03T04:06:58Z
updated: 2026-03-03T04:10:18Z
github: https://github.com/shipcrew-ai/shipcrew/issues/7
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Remove Redis and update infrastructure config

## Description
Remove the unused Redis dependency from the project and update configuration files to reflect the SQLite default.

## Acceptance Criteria
- [ ] `docker-compose.yml`: Redis service removed entirely, `redis_data` volume removed
- [ ] `docker-compose.yml`: Comment added at top explaining this is optional for production PostgreSQL
- [ ] `.env.example`: `REDIS_URL` line removed
- [ ] `.env.example`: `DATABASE_URL` default changed to `file:./prisma/dev.db`
- [ ] `.env.example`: Comment updated to explain SQLite is the default for local dev
- [ ] No Redis references remain in config files

## Technical Details

### `docker-compose.yml`
- Remove the `redis` service block
- Remove `redis_data` from the `volumes` section
- Add a comment at the top: `# Optional: Only needed if using PostgreSQL instead of SQLite`
- Keep the `postgres` service intact for production use

### `.env.example`
- Change: `DATABASE_URL=file:./prisma/dev.db`
- Remove: `REDIS_URL=redis://localhost:6379`
- Add a comment: `# For PostgreSQL: DATABASE_URL=postgresql://devteam:devteam_secret@localhost:5432/devteam`

## Dependencies
- [ ] None — independent infrastructure change

## Effort Estimate
- Size: XS
- Hours: 0.25
- Parallel: true — fully independent

## Definition of Done
- [ ] Redis completely removed from `docker-compose.yml`
- [ ] `.env.example` updated with SQLite default
- [ ] No stale Redis references in config
